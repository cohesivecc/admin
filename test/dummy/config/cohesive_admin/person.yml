duplicate: true
fields:
  prefix: collection
  name:
    type: string
    help: "This is the display name for the person."
  image:
    type: activeStorage
    multiple: false
  file:
    type: activeStorage
    multiple: false
  documents:
    type: activeStorage
    multiple: true
  email: email
  job: association
  bio: wysiwyg
  active:
    type: boolean
    label: "Is Active?"
    help: "Only active people show up in the directory."
    filter: true
