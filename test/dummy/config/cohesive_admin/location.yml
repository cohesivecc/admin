order: 2
fields:
  slug:
  image:
    type: refile
    help: "Upload a picture here"
    placeholder: "Here is some placeholder text"
  json_data: code
  addresses:
    type: association
    filter: true
  jobs: association
